services:
  db:
    image: postgres:17
    restart: always
    environment:
      POSTGRES_DB: ${PGDATABASE}
      POSTGRES_USER: ${PGUSER}
      POSTGRES_PASSWORD: ${PGPASSWORD}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    env_file:
      - .env
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${PGUSER} -d ${PGDATABASE}"]
      interval: 5s
      timeout: 5s
      retries: 5

  web:
    build: .
    container_name: django-web
    restart: always
    ports:
      - "8001:8001"
    volumes:
      - .:/app
      - media_volume:/app/media
      - static_volume:/app/staticfiles
    depends_on:
      db:
        condition: service_healthy
    env_file:
      - .env

  bot:
    build: .
    container_name: taqdimot-bot
    restart: always
    command: python bot.py
    volumes:
      - .:/app
    depends_on:
      db:
        condition: service_healthy
    env_file:
      - .env

  # ══════════════════════════════════════
  # Frontend va Nginx — keyinchalik yoqiladi
  # ══════════════════════════════════════

  # frontend:
  #   build:
  #     context: ./frontend
  #     dockerfile: Dockerfile
  #   container_name: react-frontend
  #   restart: always
  #   depends_on:
  #     - web

  # nginx:
  #   image: nginx:alpine
  #   container_name: nginx-proxy
  #   restart: always
  #   ports:
  #     - "80:80"
  #     - "443:443"
  #   volumes:
  #     - ./nginx.conf:/etc/nginx/conf.d/default.conf
  #     - /etc/letsencrypt:/etc/letsencrypt:ro
  #     - media_volume:/app/media
  #     - static_volume:/app/staticfiles
  #   depends_on:
  #     - web
  #     - frontend

volumes:
  postgres_data:
  media_volume:
  static_volume: